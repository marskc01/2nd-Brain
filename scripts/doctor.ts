const checks=[['Application',true],['Supabase',!!process.env.NEXT_PUBLIC_SUPABASE_URL&&!!process.env.SUPABASE_SERVICE_ROLE_KEY],['OpenAI',!!process.env.OPENAI_API_KEY],['Instagram',!!process.env.META_APP_SECRET&&!!process.env.META_VERIFY_TOKEN],['MCP',!!process.env.KDN_MCP_TOKEN],['Higgsfield',!!process.env.HIGGSFIELD_MCP_URL]] as const;console.log('\nKDN Brain Doctor\n');for(const[c,ok]of checks)console.log(`${ok?'✓':'!'} ${c}${ok?'':' — missing or optional configuration'}`);console.log(checks.slice(0,3).every(x=>x[1])?'\nCore system ready.':'\nComplete required configuration before live processing.');
