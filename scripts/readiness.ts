const required=['NEXT_PUBLIC_APP_URL','NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','OPENAI_API_KEY','KDN_MCP_TOKEN'];const missing=required.filter(k=>!process.env[k]);if(missing.length){console.error('Production readiness failed. Missing: '+missing.join(', '));process.exit(1)}console.log('Production-critical configuration is present. Run the live service health checks after deployment.');
