import fs from'node:fs';import crypto from'node:crypto';const body=fs.readFileSync('fixtures/instagram-reel.json');const secret=process.env.META_APP_SECRET||'development-secret';const signature='sha256='+crypto.createHmac('sha256',secret).update(body).digest('hex');const url=(process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000')+'/api/webhooks/meta/instagram';const response=await fetch(url,{method:'POST',headers:{'content-type':'application/json','x-hub-signature-256':signature},body});console.log(response.status,await response.text());process.exitCode=response.ok?0:1;
