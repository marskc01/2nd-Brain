import {NextRequest,NextResponse} from 'next/server';import {manualCaptureSchema} from '@/lib/schemas';import {ManualAdapter} from '@/lib/adapters/manual';import {saveCapture} from '@/lib/pipeline';
export async function POST(req:NextRequest){const parsed=manualCaptureSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid capture',details:parsed.error.flatten()},{status:400});try{const [normalised]=await new ManualAdapter().normalise(parsed.data);return NextResponse.json({capture:await saveCapture(normalised)},{status:202})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Capture failed'},{status:503})}}
