import{describe,it,expect,beforeEach,afterEach}from'vitest';import{opportunityScore,scoreLabel}from'../src/lib/opportunity';import{canonicaliseUrl,normaliseEntity,allowedInstagramSender}from'../src/lib/normalise';import{captureAnalysisSchema}from'../src/lib/schemas';import{verifyMetaSignature}from'../src/lib/meta';import crypto from'node:crypto';
describe('opportunity scoring',()=>{it('calculates weighted arithmetic outside the model',()=>{expect(opportunityScore({goalAlignment:10,revenuePotential:8,personalAdvantage:8,speedToPrototype:9,strategicLeverage:7,marketAccessibility:6,learningValue:8,personalInterest:10})).toBe(8.4);expect(scoreLabel(8.4)).toBe('Prototype')})});
describe('normalisation',()=>{it('canonicalises tracking URLs',()=>expect(canonicaliseUrl('https://WWW.Example.com/a/?utm_source=ig&b=2#a')).toBe('https://example.com/a?b=2'));it('merges obvious entity aliases',()=>expect(normaliseEntity('Higgsfield AI')).toBe(normaliseEntity('higgsfield.ai')))});
describe('access control',()=>{const old=process.env.INSTAGRAM_OWNER_IGSID;beforeEach(()=>process.env.INSTAGRAM_OWNER_IGSID='owner');afterEach(()=>process.env.INSTAGRAM_OWNER_IGSID=old);it('allows owner and rejects strangers',()=>{expect(allowedInstagramSender('owner')).toBe(true);expect(allowedInstagramSender('stranger')).toBe(false)})});
describe('structured output',()=>it('rejects malformed analysis',()=>expect(captureAnalysisSchema.safeParse({title:'x'}).success).toBe(false)));
describe('webhook signature',()=>it('accepts valid and rejects invalid signatures',()=>{const body=Buffer.from('{}'),secret='secret',sig='sha256='+crypto.createHmac('sha256',secret).update(body).digest('hex');expect(verifyMetaSignature(body,sig,secret)).toBe(true);expect(verifyMetaSignature(body,'sha256=bad',secret)).toBe(false)}));
